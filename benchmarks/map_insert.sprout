~ Map growth: insert 20,000 distinct keys.
~ As of v0.0.30 maps have a hash index, so this is O(n) (~780 ms -> ~57 ms).
make m = {}
make i = 0
repeat 20000 times:
    set m["k" + i] = i
    set i += 1
show length(keys(m))
