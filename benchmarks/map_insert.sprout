~ Map growth: insert 20,000 distinct keys.
~ NOTE: maps currently use a linear key scan, so this is O(n^2) — a known
~ optimization target (a hash table would make it O(n)). See BENCHMARKS.md.
make m = {}
make i = 0
repeat 20000 times:
    set m["k" + i] = i
    set i += 1
show length(keys(m))
